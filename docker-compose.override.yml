version: "3.7"
# for sharing a file swagger.json made by build of api container
# volumes:
#     swaggerDefFile:
services:
    sensors_api:
        build:
            args:
                BUILD_TYPE: OOOOOOOO
        ports:
            - "443:5000" # questionalbe part. Investigate later. Doesnt work/needed.